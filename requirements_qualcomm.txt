# Requirements for Qualcomm NPU Pose Detection
# For use with Qualcomm Snapdragon X Elite devices

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# ONNX Runtime with QNN Execution Provider
# Note: Use ARM64 version for inference on Qualcomm NPU devices
# Use x64 version for model quantization on x64 machines
onnxruntime-qnn>=1.18.0

# Optional: For model optimization and quantization
# Uncomment if you need to quantize models
# onnx>=1.14.0
# onnxruntime>=1.16.0

# Typing support (for older Python versions)
typing-extensions>=4.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0 